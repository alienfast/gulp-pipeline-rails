import 'bootstrap'
import 'bootstrap-material-design'

let bg = 'foo/bar/startransparent.gif'   // test for digest - make sure it is used to avoid rollup's tree shaking
console.log(bg)  // eslint-disable-line no-console
